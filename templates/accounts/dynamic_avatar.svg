<svg
  xmlns="http://www.w3.org/2000/svg"
  version="1.1"
  width="{{ size }}"
  height="{{ size }}"
  class="avatar colour-{{ colour_ix }}">
  <text
    x="50%"
    y="50%"
    text-anchor="middle"
    dy="0.4em"
    font-size="{{ font_size }}"
    fill="#FFFFFF">
      {{letter}}
    </text>
</svg>